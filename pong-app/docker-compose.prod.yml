version: "3.9"

services:
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile.prod
    ports:
      - "443:443"
    depends_on:
      - backend
    networks:
      - pong-network

  ngrok:
    build: ./ngrok
    container_name: ngrok
    env_file: ./ngrok/.env
    environment:
      - APP_MODE=prod
      - NGROK_HOST=frontend
      - NGROK_PORT=443
    depends_on:
      - frontend
      - backend
    ports:
      - "4040:4040"
    networks:
      - pong-network

